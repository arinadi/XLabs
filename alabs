#!/bin/bash
# arinanoLabs TUI — resolve symlink so the launcher works from anywhere
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
cd "$SCRIPT_DIR"
python3 -c "
import sys, os
sys.path.insert(0, os.getcwd())
from installer.npyscreen_app import run_npyscreen
run_npyscreen()
"
