#!/bin/bash
# arinanoLabs TUI launcher.
# readlink -f resolves ~/bin/alabs back to the repo, so this works from
# anywhere and keeps working after a git pull.
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
exec env PYTHONPATH="$SCRIPT_DIR" python3 -m installer.app "$@"
