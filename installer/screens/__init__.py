"""XLabs TUI screens, one module per screen (or small family of screens).

app.py just wires MainScreen up as XLabsApp's entry point — split out so
working on one screen doesn't mean reading every other one first.
"""
