#!/bin/bash
# 📱 arinanoX — MATE Theme (replaces old XFCE config)
# Orchis Dark Material is baked into the image layer.
# Config: ~/.config/dconf/ (dconf database)

echo ">>> MATE theme is pre-configured in the image."
echo "    To customize: use dconf-editor or gsettings."
